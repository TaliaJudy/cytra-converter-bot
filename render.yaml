services:
  - type: worker
    name: cytra-converter-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python cytra_converter_bot.py"
    envVars:
      - key: TG_BOT_TOKEN
        sync: false  # you’ll set it manually in Render dashboard
